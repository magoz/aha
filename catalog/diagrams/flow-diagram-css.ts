import { DIAGRAM_CSS } from './diagram-css.js'

/**
 * Flow-diagram styles. The shared diagram kit already covers boxes,
 * edges, groups and selection; nothing flow-specific remains.
 */

export const FLOW_DIAGRAM_CSS = `${DIAGRAM_CSS}`
